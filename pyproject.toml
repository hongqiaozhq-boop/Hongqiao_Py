[project]
name = "pygame"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hongqiao", email = "hongqiao.zhq@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.15",
    "langchain-deepseek>=1.1.0",
]

[project.scripts]
pygame = "pygame:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
